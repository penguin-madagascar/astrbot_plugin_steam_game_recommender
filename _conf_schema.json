{
  "model_and_access": {
    "type": "object",
    "description": "模型与鉴权",
    "hint": "配置偏好解析、推荐理由和 Steam 游戏库访问",
    "items": {
      "llm_provider_id": {
        "type": "string",
        "description": "用于偏好解析和推荐理由的 LLM 提供商",
        "hint": "‼️留空时尝试使用当前会话模型；偏好解析失败会降级为关键词规则，单款理由失败会降级为可信证据规则理由。",
        "_special": "select_provider",
        "default": ""
      },
      "semantic_verification_batch_size": {
        "type": "int",
        "description": "语义特征核验的单批候选数",
        "hint": "每批提交给模型核验的候选游戏数量；运行时限制为 1–10。",
        "default": 5
      },
      "steam_api_key": {
        "type": "string",
        "description": "Steam Web API Key",
        "hint": "用于 /gamerec 的游戏库过滤和 /randomrec 调用 Steam GetOwnedGames；留空时账号绑定仍可用，但无法读取游戏库。可前往 [Steam Web API Key 申请页](https://steamcommunity.com/dev/apikey) 获取。",
        "default": ""
      }
    }
  },
  "price_and_region": {
    "type": "object",
    "description": "价格与地区",
    "hint": "配置 Steam 价格增强说明和默认查询地区",
    "items": {
      "steam_price_heybox_notice": {
        "type": "text",
        "description": "Steam 价格增强说明",
        "hint": "这是提示信息，无需配置；安装 astrbot_plugin_steam_price_heybox 后会自动启用。",
        "default": "无需配置。安装 astrbot_plugin_steam_price_heybox 后，本插件会自动启用指定区域价格查询，返回 Steam 当前价、历史最低和最近促销；未安装时仍可正常推荐游戏。",
        "_readonly": true,
        "obvious_hint": true
      },
      "default_region": {
        "type": "string",
        "description": "默认地区代码",
        "hint": "未在命令中指定地区时使用；支持 CN、US、JP 等两字母代码，查询可用 -US、国区、美区、日区等覆盖。",
        "default": "CN"
      }
    }
  },
  "recommendation_and_scoring": {
    "type": "object",
    "description": "推荐与评分",
    "hint": "配置推荐数量和索引缓存；/gamerec 使用固定的锚点分层、Wilson 口碑与评测规模评分，随机推荐门槛仅供 /randomrec 使用。",
    "items": {
      "max_results": {
        "type": "int",
        "description": "默认推荐数量上限",
        "hint": "每次默认返回的推荐条数，范围 1-10；用户明确要求数量时会优先采用用户数量但不超过该上限。",
        "default": 10
      },
      "steam_index_ttl_hours": {
        "type": "int",
        "description": "Steam 推荐索引缓存有效期（小时）",
        "hint": "Steam 推荐会优先使用本地索引；超过该时间后会尝试用 Steam 公开数据刷新索引。",
        "default": 168
      },
      "steam_min_review_count": {
        "type": "int",
        "description": "随机推荐最低评测数量",
        "hint": "仅用于 /randomrec 的最低 Steam 评测数量门槛。",
        "default": 50
      },
      "steam_min_positive_ratio": {
        "type": "float",
        "description": "随机推荐最低好评率",
        "hint": "仅用于 /randomrec 的最低 Steam 好评率门槛。",
        "default": 0.65
      }
    }
  },
  "cache_and_network": {
    "type": "object",
    "description": "缓存与网络",
    "hint": "配置 Steam 数据缓存和网络请求超时",
    "items": {
      "cache_ttl_hours": {
        "type": "int",
        "description": "游戏数据查询缓存有效期（小时）",
        "hint": "Steam 查询结果的缓存时间；调大可减少请求，调小可获得更新数据。",
        "default": 24
      },
      "timeout_seconds": {
        "type": "int",
        "description": "HTTP 请求超时时间",
        "hint": "Steam 和价格查询的单次 HTTP 请求超时时间，单位秒。",
        "default": 15
      },
      "reuse_identical_query_cache": {
        "type": "bool",
        "description": "复用顺序相同的推荐查询缓存",
        "hint": "关闭后，顺序相同的推荐会重新尝试 Steam 发现召回与语义核验；AppDetails、评测、商店标签、标签词表、发行状态、价格、账号、本地实体索引和 /gamerec_retry 记忆仍会缓存；实时召回失败时可使用最多 7 天的旧发现快照。",
        "default": false
      }
    }
  }
}
